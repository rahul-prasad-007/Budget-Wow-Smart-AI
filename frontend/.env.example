# Local development — Vite proxies /api to http://localhost:5000
VITE_API_URL=/api

# Production (Vercel) — set in Vercel dashboard Environment Variables:
# VITE_API_URL=https://budget-wow-smart-ai.onrender.com/api
